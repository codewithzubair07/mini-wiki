# mini-wiki dependencies
# All tools are written using the Python standard library only,
# so no external packages are required for the core workflows.
#
# Optional: install these if you want to extend the tools with LLM API calls
# openai>=1.0.0
# anthropic>=0.20.0
# PyYAML>=6.0
